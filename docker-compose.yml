services:
  selah:
    build: .
    ports:
      - "4610:4610"
    volumes:
      - selah-data:/app/data
      - selah-backups:/app/backups
    environment:
      - DATABASE_URL=file:/app/data/selah.db
      - NODE_ENV=production
volumes:
  selah-data:
  selah-backups:
